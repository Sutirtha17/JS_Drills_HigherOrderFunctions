import each from "../each.js";
import { items } from "../../dataset/dataset.js";
const cb = (value, index) => {
  // console.log(value);   // used for testing purpose
  /* ADD YOUR CODE HERE*/
};
each(items, cb);
